from pathlib import Path
FILES = {'skills/signal-core/profiles/roblox_studio/MANIFEST.sha256': '346f16e180a7b47a1f1cce59c0dfdd850c1341c3b4ed36954b720b2205dedcd5  ARCHITECTURE.md\n51abbc4e80d672b49cd28913060e9455509c9279402494f37757371c8e46c357  BENCHMARK_RESULTS.md\nd9d1c28c74bd75ed32776c029dd53da79539f66a495e5ece0d96ef2aafe311ff  COMPLETION_REPORT.md\nc8dc44424029dde1974bc0ea4e1035c908f33041581b4a22aa01dc4a074bdfae  MIGRATION.md\n78d1021f323be6b4977879a99a0a73a45c6cff346a89c721cd2d2b90e66fa3e1  __init__.py\n5711212b09f81cc623f851980b7b8f6a4e890dff1b2f845b1d2507c1de1527ef  activation.py\n2ba0d2ea750794a86550707ecd48fdae8df76505e51c7bface4e31d5246e281b  adapters/base.py\nd1bd9a2ce9e57cc55242467b4ce0106b1bd4ee5e7c8fbfe3f05845ad6fed5a3e  adapters/live/__init__.py\n5b34adae4f637f25e52e30fef9ee049e45fbe82ddcd64a00c6ec46eef2e77af1  adapters/simulated/__init__.py\n93d3624e222cf0437178963829ce4d2b2fa31c02ec9d29a2fd27244359189acb  adapters/transcript/__init__.py\n6aa51b5ee6b16db96703db041ae98d291c8aac8679111fbe164fbc5d32ca5d82  budgets.py\nc3c7ab34323075351f43ba9bbed042f8608feed5317a1c867cce0161feb79fa9  capabilities.py\n3bd51e724a5b0d82cacaaf985abab6886933d136230654e654f2d5f8178e65e7  capability_graph.py\nd4b59291c4b0c522832186845d3bb76d95a87a8ccc15ed0fa562dd45b54a51e9  context_knapsack.py\nc379de02224e13fcb1ae6ec4bdbaa34341f3afec6b68e7e732531afda108048e  datamodel.py\n1669554a513a69fa168ce8f7abdf23885c66bcda5d25fb6873fd78c2ced7eede  errors.py\nff5d8e75427d0ad9e03d5756ed9126b1e8336f88d1d7482fdfe64a36fe24eee0  evidence_ledger.py\n023bfd53b4119c9a80db4b1201e1ce1f0546c77798a548928f0cf57428e72450  memory.py\n66345327feb277de5cef913b156ba130b28b498e8f17e481b57c9f2b9b0aa765  output_virtualization.py\n2d6f2c8c3c15d26c949f64fa711da116b83aad22854056efdf466a6bb48c588b  profile.json\n42cb00f6c22afaf59445596297a9fc1889e9dcff0fba86c5170de01304823624  profile.py\nddbac8e2e350d2f1031f22f754276e7e1a5ce4df5f6eb0d34c4dc887899abff2  registries.py\nb1c70d42e0651deb7d6b82f1a3e1ebb674e1eb16ffd81d3e1d5a2966b247a0e2  scheduler.py\nd702dcde115f8c59a3b8e108df3b7a78bfb32e32af0053105d4218015badc7c8  schemas/benchmark.schema.json\n90525ed616447befff8bca10e8e130ff7dc04a3f0c3b11158f6b4804494fff5c  schemas/task-state.schema.json\n835969367cdcc87be4586cd91fe2d1f2693b1196301f0d01ac70a5f4562c8b61  task_state.py\n1297795e60fbfbe3a1ed77e244b55f5f3f24d02f25fd20adde47e8f50bd505c3  telemetry.py\na19cffabc6bf4482e919110b60ef2682a3d70229d0375036324ba0a574a62394  validators.py\n09ad88e0368aba3837d58ddc5fba9e9a1f895e0008c6c2a86c1f90484f16cc9e  workflow.py\n', 'skills/signal-core/profiles/roblox_studio/MIGRATION.md': '# Migration Notes\n\nThe clean materialized profile replaces the documentation-only/payload branch. Existing version remains `0.0.1`.\n\nTask-state schema version 1 can be migrated to version 2 with `migrate_task_state`. Activation envelope schema version 1 is not accepted because security-sensitive unknown or omitted identity fields must fail closed.\n', 'skills/signal-core/profiles/roblox_studio/__init__.py': '"""Fail-closed Roblox Studio orchestration profile for SignalCore 0.0.1."""\n\nfrom .profile import RobloxStudioOrchestrator, OrchestrationResult\nfrom .task_state import RobloxTaskState\n\n__all__ = ["RobloxStudioOrchestrator", "OrchestrationResult", "RobloxTaskState"]\n', 'skills/signal-core/profiles/roblox_studio/activation.py': 'from __future__ import annotations\n\nimport base64\nimport hashlib\nimport hmac\nimport json\nimport sqlite3\nimport time\nfrom dataclasses import dataclass\nfrom pathlib import Path\nfrom typing import Any, Callable, Iterable, Mapping\n\nfrom .errors import ActivationError, ReplayDetected\n\nPROFILE_ID = "roblox_studio"\nPROFILE_VERSION = "0.0.1"\nENVELOPE_SCHEMA_VERSION = 2\nHOST = "roblox_studio"\nTRANSPORT = "roblox_studio_bridge"\n_MAX_ENVELOPE_BYTES = 16_384\n_REQUIRED_FIELDS = {\n    "schema_version", "profile_id", "profile_version", "host", "transport",\n    "transport_identity", "studio_session_id", "place_id", "project_id",\n    "project_fingerprint", "studio_process_id", "capabilities", "issued_at",\n    "expires_at", "nonce", "signature",\n}\n\n\ndef _b64e(raw: bytes) -> str:\n    return base64.urlsafe_b64encode(raw).decode("ascii").rstrip("=")\n\n\ndef _canonical(payload: Mapping[str, Any]) -> bytes:\n    return json.dumps(payload, ensure_ascii=False, sort_keys=True, separators=(",", ":")).encode("utf-8")\n\n\ndef sign_payload(payload: Mapping[str, Any], key: bytes) -> str:\n    if len(key) < 32:\n        raise ActivationError("pairing key must contain at least 32 bytes")\n    return _b64e(hmac.new(key, _canonical(payload), hashlib.sha256).digest())\n\n\n@dataclass(frozen=True, slots=True)\nclass AuthorizedSession:\n    session_id: str\n    place_id: str\n    project_id: str\n    project_fingerprint: str\n    studio_process_id: int\n    capabilities: tuple[str, ...]\n    transport_identity: str\n    issued_at: int\n    expires_at: int\n    nonce: str\n\n\nclass ReplayStore:\n    def __init__(self, path: Path) -> None:\n        self.path = Path(path)\n        self.path.parent.mkdir(parents=True, exist_ok=True)\n\n    def consume(self, nonce: str, session_id: str, expires_at: int, now: int) -> None:\n        nonce_hash = hashlib.sha256(nonce.encode("utf-8")).hexdigest()\n        connection = sqlite3.connect(self.path, timeout=15, isolation_level=None)\n        try:\n            connection.execute("PRAGMA journal_mode=WAL")\n            connection.execute("PRAGMA busy_timeout=15000")\n            connection.execute("CREATE TABLE IF NOT EXISTS nonces(hash TEXT PRIMARY KEY, session_id TEXT NOT NULL, expires_at INTEGER NOT NULL, consumed_at INTEGER NOT NULL)")\n            connection.execute("BEGIN IMMEDIATE")\n            connection.execute("DELETE FROM nonces WHERE expires_at < ?", (now - 300,))\n            try:\n                connection.execute("INSERT INTO nonces VALUES(?,?,?,?)", (nonce_hash, session_id, expires_at, now))\n            except sqlite3.IntegrityError as exc:\n                connection.execute("ROLLBACK")\n                raise ReplayDetected("activation nonce was already consumed") from exc\n            connection.execute("COMMIT")\n        finally:\n            if connection.in_transaction:\n                connection.execute("ROLLBACK")\n            connection.close()\n\n\ndef mint_envelope(\n    *, key: bytes, session_id: str, place_id: str, project_id: str,\n    project_fingerprint: str, studio_process_id: int, capabilities: Iterable[str],\n    transport_identity: str, now: int | None = None, ttl_seconds: int = 60,\n    nonce: str | None = None,\n) -> dict[str, Any]:\n    if not 1 <= ttl_seconds <= 120:\n        raise ActivationError("ttl_seconds must be in [1, 120]")\n    issued = int(time.time()) if now is None else int(now)\n    payload = {\n        "schema_version": ENVELOPE_SCHEMA_VERSION,\n        "profile_id": PROFILE_ID,\n        "profile_version": PROFILE_VERSION,\n        "host": HOST,\n        "transport": TRANSPORT,\n        "transport_identity": str(transport_identity),\n        "studio_session_id": str(session_id),\n        "place_id": str(place_id),\n        "project_id": str(project_id),\n        "project_fingerprint": str(project_fingerprint),\n        "studio_process_id": int(studio_process_id),\n        "capabilities": sorted(set(map(str, capabilities))),\n        "issued_at": issued,\n        "expires_at": issued + ttl_seconds,\n        "nonce": nonce or hashlib.sha256(f"{session_id}:{issued}:{time.time_ns()}".encode()).hexdigest(),\n    }\n    return {**payload, "signature": sign_payload(payload, key)}\n\n\ndef verify_envelope(\n    envelope: Mapping[str, Any] | None, *, key: bytes, replay_store: ReplayStore,\n    allowed_capabilities: Iterable[str], expected_transport_identity: str,\n    expected_process_id: int, expected_place_id: str, expected_project_id: str,\n    expected_project_fingerprint: str, process_attestor: Callable[[int], bool],\n    now: int | None = None, clock_skew_seconds: int = 5,\n) -> AuthorizedSession:\n    if not envelope:\n        raise ActivationError("signed Roblox Studio activation envelope required")\n    try:\n        encoded_size = len(json.dumps(envelope, ensure_ascii=False).encode("utf-8"))\n    except (TypeError, ValueError) as exc:\n        raise ActivationError("activation envelope is not JSON serializable") from exc\n    if encoded_size > _MAX_ENVELOPE_BYTES:\n        raise ActivationError("activation envelope is oversized")\n    if set(envelope) != _REQUIRED_FIELDS:\n        raise ActivationError("activation envelope fields are invalid")\n    payload = {key_name: envelope[key_name] for key_name in envelope if key_name != "signature"}\n    supplied = str(envelope["signature"])\n    if not hmac.compare_digest(supplied, sign_payload(payload, key)):\n        raise ActivationError("activation signature mismatch")\n    if payload["schema_version"] != ENVELOPE_SCHEMA_VERSION:\n        raise ActivationError("unsupported activation schema")\n    if payload["profile_id"] != PROFILE_ID or payload["profile_version"] != PROFILE_VERSION:\n        raise ActivationError("cross-profile activation rejected")\n    if payload["host"] != HOST or payload["transport"] != TRANSPORT:\n        raise ActivationError("ordinary CLI or IDE activation is forbidden")\n    if payload["transport_identity"] != expected_transport_identity:\n        raise ActivationError("transport identity mismatch")\n    current = int(time.time()) if now is None else int(now)\n    issued, expires = int(payload["issued_at"]), int(payload["expires_at"])\n    if issued > current + clock_skew_seconds:\n        raise ActivationError("activation was issued in the future")\n    if expires < current - clock_skew_seconds:\n        raise ActivationError("activation expired")\n    if expires <= issued or expires - issued > 120:\n        raise ActivationError("activation TTL is invalid")\n    process_id = int(payload["studio_process_id"])\n    if process_id != expected_process_id or not process_attestor(process_id):\n        raise ActivationError("Studio process attestation failed")\n    if str(payload["place_id"]) != str(expected_place_id):\n        raise ActivationError("place identity mismatch")\n    if str(payload["project_id"]) != str(expected_project_id):\n        raise ActivationError("project identity mismatch")\n    if str(payload["project_fingerprint"]) != str(expected_project_fingerprint):\n        raise ActivationError("project fingerprint mismatch")\n    capabilities_raw = payload["capabilities"]\n    if not isinstance(capabilities_raw, list) or len(capabilities_raw) > 64:\n        raise ActivationError("malformed capability set")\n    capabilities = tuple(sorted(set(map(str, capabilities_raw))))\n    if not capabilities or not set(capabilities).issubset(set(map(str, allowed_capabilities))):\n        raise ActivationError("capability escalation rejected")\n    nonce = str(payload["nonce"])\n    session_id = str(payload["studio_session_id"])\n    if min(len(nonce), len(session_id), len(str(payload["project_fingerprint"]))) < 8:\n        raise ActivationError("activation identity fields are incomplete")\n    replay_store.consume(nonce, session_id, expires, current)\n    return AuthorizedSession(\n        session_id=session_id,\n        place_id=str(payload["place_id"]),\n        project_id=str(payload["project_id"]),\n        project_fingerprint=str(payload["project_fingerprint"]),\n        studio_process_id=process_id,\n        capabilities=capabilities,\n        transport_identity=str(payload["transport_identity"]),\n        issued_at=issued,\n        expires_at=expires,\n        nonce=nonce,\n    )\n', 'skills/signal-core/profiles/roblox_studio/adapters/base.py': 'from __future__ import annotations\n\nfrom dataclasses import dataclass\nfrom typing import Any, Mapping, Protocol\n\n\n@dataclass(frozen=True, slots=True)\nclass EngineRequest:\n    capability: str\n    task_id: str\n    payload: Mapping[str, Any]\n\n\n@dataclass(frozen=True, slots=True)\nclass EngineResponse:\n    engine_id: str\n    engine_version: str\n    capability: str\n    status: str\n    payload: Mapping[str, Any]\n    evidence_references: tuple[str, ...]\n    validator_results: Mapping[str, bool]\n\n\nclass EngineAdapter(Protocol):\n    engine_id: str\n    engine_version: str\n    mode: str\n\n    def execute(self, request: EngineRequest) -> EngineResponse: ...\n', 'skills/signal-core/profiles/roblox_studio/adapters/live/__init__.py': 'from __future__ import annotations\n\nfrom typing import Callable\n\nfrom ..base import EngineRequest, EngineResponse\nfrom ...activation import AuthorizedSession\nfrom ...errors import ActivationError\n\n\nclass LiveStudioAdapter:\n    engine_id = "studio_bridge"\n    engine_version = "1.0.0-live-contract"\n    mode = "LIVE"\n\n    def __init__(self, *, enabled: bool, session: AuthorizedSession | None, transport: Callable[[EngineRequest], EngineResponse] | None) -> None:\n        self.enabled = enabled\n        self.session = session\n        self.transport = transport\n\n    def execute(self, request: EngineRequest) -> EngineResponse:\n        if not self.enabled or self.session is None or self.transport is None:\n            raise ActivationError("live Studio adapter is disabled until signed activation and explicit configuration are present")\n        if request.capability not in self.session.capabilities:\n            raise ActivationError("live adapter capability is not authorized")\n        return self.transport(request)\n', 'skills/signal-core/profiles/roblox_studio/adapters/simulated/__init__.py': 'from __future__ import annotations\n\nimport hashlib\nimport json\n\nfrom ..base import EngineRequest, EngineResponse\n\n\nclass SimulatedStudioAdapter:\n    engine_id = "studio_bridge"\n    engine_version = "1.0.0-simulated"\n    mode = "SIMULATED"\n\n    def execute(self, request: EngineRequest) -> EngineResponse:\n        canonical = json.dumps(dict(request.payload), sort_keys=True, separators=(",", ":"))\n        artifact_hash = hashlib.sha256((request.capability + canonical).encode()).hexdigest()\n        payload = {\n            "task_id": request.task_id,\n            "capability": request.capability,\n            "artifact_hash": artifact_hash,\n            "mode": self.mode,\n            "postcondition": "simulated-ok",\n        }\n        return EngineResponse(self.engine_id, self.engine_version, request.capability, "SUCCEEDED", payload, (f"simulated:{artifact_hash}",), {"response_schema": True, "artifact_hash": True})\n', 'skills/signal-core/profiles/roblox_studio/adapters/transcript/__init__.py': 'from __future__ import annotations\n\nfrom dataclasses import dataclass\nfrom typing import Any, Iterable, Mapping\n\nfrom ..base import EngineRequest, EngineResponse\nfrom ...errors import ValidationError\n\n\n@dataclass(frozen=True, slots=True)\nclass TranscriptEvent:\n    sequence: int\n    event_type: str\n    payload: Mapping[str, Any]\n\n\nclass TranscriptAdapter:\n    engine_id = "studio_bridge"\n    engine_version = "1.0.0-transcript"\n    mode = "TRANSCRIPT"\n\n    def __init__(self, events: Iterable[TranscriptEvent]) -> None:\n        self.events = tuple(events)\n        sequences = [event.sequence for event in self.events]\n        if sequences != list(range(len(sequences))):\n            raise ValidationError("transcript event ordering is invalid")\n\n    def execute(self, request: EngineRequest) -> EngineResponse:\n        matching = [event for event in self.events if event.event_type == "response" and event.payload.get("capability") == request.capability]\n        if not matching:\n            raise ValidationError("transcript has no matching response")\n        payload = dict(matching[0].payload)\n        if payload.get("status") not in {"SUCCEEDED", "FAILED", "BLOCKED", "PARTIAL"}:\n            raise ValidationError("transcript response status is invalid")\n        return EngineResponse(self.engine_id, self.engine_version, request.capability, str(payload["status"]), payload, tuple(payload.get("evidence_references", ())), {"transcript_schema": True})\n'}
for relative, content in FILES.items():
    path=Path(relative)
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(content, encoding='utf-8')
